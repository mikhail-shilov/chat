const initialState = {
  token: '123123test',
  user: {
    name: 'LocalUser',
    id: '321321ff'
  }
}

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}
