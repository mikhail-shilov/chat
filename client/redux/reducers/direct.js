const initialState = {
  token: '',
  user: {
    name: ''
  },
  input: {
    login: '',
    password: ''
  }
}

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}
